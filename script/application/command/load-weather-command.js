export default class LoadWeatherCommand {
    /**
     * @param {number} lat
     * @param {number} lon
     */
    static execute(lat, lon) {
        console.log(lat);
        console.log(lon);
    }
}
